version: '3.8'

services:
  coupon-api:
    build: .
    container_name: coupon-api
    ports:
      - "8080:8080"
    environment:
      - SPRING_PROFILES_ACTIVE=default
      - SERVER_PORT=8080
    networks:
      - coupon-network

networks:
  coupon-network:
    driver: bridge
